'use strict';

var AppSettings = {
    appTitle: 'REST Angular',
    apiUrl: '/api/v1',
    viewsPath: 'build/views/',
    '_': window._,
    pageLimit: 10,
    queryParams: '?skip=0&limit=10'
};

module.exports = AppSettings;